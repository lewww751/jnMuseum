# AGENTS.md

## Agent skills

### Issue tracker

Issue 以 markdown 文件形式存放在仓库 `.scratch/<feature>/` 目录下（本地 markdown 跟踪器，本仓库无远程仓库）。See `docs/agents/issue-tracker.md`.

### Triage labels

使用五个标准 triage 角色标签（`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`），标签字符串与角色名一致。See `docs/agents/triage-labels.md`.

### Domain docs

单上下文（single-context）布局：根目录 `CONTEXT.md` + `docs/adr/`。See `docs/agents/domain.md`.
